const nome = `Fagner Ferreira`;
const idade = 18;
const peso = 65;
const altura = 1.65;
let imc;
let nascimento;

imc = peso / (altura * altura)
nascimento = 2023 - 18
console.log(`${nome} tem ${idade} anos, pesa ${peso} kg, tem ${altura}, sua data de nascimento é ${nascimento} de altura e seu IMC é de ${imc}`);